import * as Action from "./storeReducer";

const initilState = 0;

export default function PostStoredReducer(state = initilState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
